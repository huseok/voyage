# 阿里云香港轻量应用服务器（Ubuntu22\.04）操作手册（Docker\+后端\+PgSQL部署专用）

# 一、前言

适用环境：阿里云香港轻量应用服务器 \+ Ubuntu 22\.04 纯净版（64位）

核心用途：部署 Docker \+ 后端项目 \+ PostgreSQL 数据库 \+ 前端静态页面（外贸场景，欧美为主、全球可访问）

前置准备：已购买阿里云轻量应用服务器（香港地区），记住阿里云账号登录信息、服务器公网IP。

注意事项：全程复制命令执行，避免手动输入出错；所有操作均在 root 权限下完成，无需额外切换账号。

# 二、前期准备（必做步骤）

## 2\.1 登录阿里云轻量服务器控制台

1. 打开阿里云官网（https://www\.aliyun\.com/），登录自己的阿里云账号。

2. 在顶部导航栏搜索「轻量应用服务器」，点击进入控制台。

3. 在服务器列表中，找到已购买的「香港地区 Ubuntu22\.04」服务器，点击进入「实例详情」页。

## 2\.2 重置服务器root密码（关键步骤）

1. 在「实例详情」页顶部，点击「重置密码」按钮。

2. 输入新的 root 密码（建议包含大小写字母、数字、特殊符号，复杂度越高越安全），确认密码后点击「确定」。

3. 密码重置成功后，记住该密码（后续远程登录需要）。

4. 在「实例详情」页，找到「公网IP」，记录下来（后续远程连接、域名解析需要）。

## 2\.3 配置防火墙（仅开放必要端口，安全第一）

外贸服务器需严格控制端口开放，避免被攻击，仅开放3个必要端口：22（SSH远程连接）、80（HTTP网站访问）、443（HTTPS加密访问）。

1. 在服务器详情页，找到左侧菜单「防火墙」，点击进入。

2. 点击「添加规则」，按以下信息配置3条规则，每条规则单独添加：
        

    - 规则1：端口 22，协议 TCP，描述「SSH远程连接」，来源默认「0\.0\.0\.0/0」（允许所有IP远程，若需更安全，可限制自己本地IP）。

    - 规则2：端口 80，协议 TCP，描述「HTTP访问」，来源「0\.0\.0\.0/0」（允许全球用户访问网站）。

    - 规则3：端口 443，协议 TCP，描述「HTTPS访问」，来源「0\.0\.0\.0/0」（加密访问，外贸必备）。

3. 确认所有规则添加完成，其他未开放端口保持默认关闭状态。

# 三、远程连接服务器（两种方式，任选其一）

## 3\.1 方式1：阿里云网页端直接连接（最简单，无需装软件）

1. 回到服务器「实例详情」页，点击顶部「远程连接」按钮，选择「Workbench远程连接」。

2. 登录账号选择「root」，输入第2\.2步重置的密码，点击「登录」。

3. 登录成功后，将看到黑色命令行界面（提示符为 root@xxx:\~\#），即表示连接成功。

## 3\.2 方式2：本地软件连接（推荐，操作更便捷）

适用软件：Xshell、FinalShell、Putty（任选一种，以FinalShell为例）。

1. 下载并安装FinalShell（官网：https://www\.hostbuf\.com/），打开软件。

2. 点击「新建连接」，选择「SSH连接」，填写以下信息：

    - 主机：填写服务器公网IP（第2\.2步记录的IP）。

    - 端口：22（默认，无需修改）。

    - 用户名：root。

    - 密码：第2\.2步重置的root密码。

3. 点击「确定」，双击连接，出现「root@xxx:\~\#」提示符，即表示连接成功。

# 四、服务器初始化（必做，确保环境干净）

登录服务器后，首先更新系统软件包，避免因旧版本导致后续安装出错。

1. 在命令行中，复制以下命令，粘贴后按回车执行（全程无需手动输入）：
        `apt update \&amp;\&amp; apt upgrade \-y`

2. 执行过程中，若出现「Do you want to continue? \[Y/n\]」，直接按「y」\+回车即可。

3. 等待命令执行完成（约1\-5分钟，取决于网络速度），出现命令提示符即表示更新完成。

# 五、安装Docker \+ Docker Compose（核心环境）

你的后端、PgSQL均通过Docker部署，因此需先安装Docker和Docker Compose，以下命令一键完成安装。

1. 复制以下完整命令，粘贴到命令行，按回车执行：
        `apt install docker\.io docker\-compose \-y \&amp;\&amp; systemctl start docker \&amp;\&amp; systemctl enable docker`

2. 等待安装完成（约3\-10分钟，取决于网络），期间无需任何操作。

3. 验证安装是否成功：分别执行以下两条命令，若能输出版本号，即表示安装成功。
        `\# 验证Docker版本
docker \-v
\# 验证Docker Compose版本
docker\-compose \-v`示例输出（版本号可能略有差异，不影响使用）：

Docker version 20\.10\.21, build 20\.10\.21\-0ubuntu1\~22\.04\.1
          
docker\-compose version 1\.29\.2, build unknown

# 六、后续部署准备（衔接你的项目）

当前已完成服务器基础环境搭建（防火墙、Docker、系统更新），接下来可部署你的后端、PostgreSQL、前端，需你提供以下信息，以便后续配置：

1. 后端项目类型（如Java、Python、Node\.js等）。

2. 后端是否有现成的Docker镜像（或Dockerfile）。

3. 前端是否为静态页面（如Vue、React打包后的dist文件）。

# 七、常见问题排查

## 7\.1 远程连接失败

- 原因1：防火墙未开放22端口 → 回到第2\.3步，确认22端口规则已添加。

- 原因2：密码错误 → 回到第2\.2步，重新重置root密码。

- 原因3：公网IP输入错误 → 核对服务器「实例详情」页的公网IP。

## 7\.2 Docker安装失败

- 原因：系统软件包未更新 → 重新执行第4步的系统更新命令，再重新安装Docker。

- 解决命令：apt update \&amp;\&amp; apt install docker\.io docker\-compose \-y（重新执行安装命令）。

## 7\.3 验证Docker版本时提示「command not found」

执行命令：systemctl restart docker，重启Docker服务后，再重新验证版本。

# 八、注意事项（外贸服务器重点）

1. 禁止开放多余端口，仅保留22、80、443，避免被恶意扫描和攻击。

2. PostgreSQL数据库部署后，禁止对外开放3306端口，仅允许Docker内网访问（后续配置会说明）。

3. 定期备份数据库（后续会添加定时备份命令），避免数据丢失。

4. 服务器无需装任何面板（如宝塔），避免占用内存、与Docker冲突。

> （注：文档部分内容可能由 AI 生成）
